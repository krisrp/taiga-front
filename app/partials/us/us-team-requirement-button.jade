label.button-gray.team-requirement(
    for="team-requirement"
    class!=" <% if(canEdit){ %>editable<% }; %> <% if(isRequired){ %>active<% }; %>"
    title="{{ 'COMMON.TEAM_REQUIREMENT' | translate }}"
)
    include ../../svg/team-requirement.svg

input(
    type="checkbox"
    id="team-requirement"
    name="team-requirement"
)
